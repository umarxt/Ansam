# دليل تصميم أنسام — مرجع إلزامي

> هذا الملف هو المرجع الوحيد لقرارات التصميم في هذا المشروع.
> أي كود واجهة جديد أو معدّل يجب أن يلتزم به. عند التعارض بين هذا الملف وأي كود قائم، هذا الملف هو الصحيح.

---

## 1. عن المشروع

**العميل:** أنسام — خدمات التكييف والتبريد وصيانة المطابخ التجارية والأفران الكبيرة.

**الجمهور:** قطاع الأعمال (B2B) — مدراء تشغيل ومشتريات في المنشآت والمطاعم والسلاسل التجارية.

**هدف صفحة الهبوط:** بناء الثقة وتحصيل طلبات عروض أسعار لعقود صيانة سنوية. ليست صفحة طلب فني طارئ.

**اللغة:** العربية، اتجاه `rtl`.

---

## 2. توكنز الألوان

```css
:root {
  --ansam-navy:        #12283F;  /* أساسي: عناوين، خلفيات داكنة، الشعار */
  --ansam-slate:       #486072;  /* نص ثانوي، نص الفقرات */
  --ansam-blue:        #2E6FA8;  /* لون الفعل — حصري للأزرار والروابط */
  --ansam-blue-light:  #D6E4F0;  /* خلفيات خفيفة، حدود، hover */
  --ansam-gray:        #8A96A3;  /* نص مساعد وتسميات صغيرة فقط */
  --ansam-cream:       #FAF9F6;  /* خلفية الصفحة */
  --ansam-white:       #FFFFFF;  /* خلفية الأقسام المتناوبة والبطاقات */
}
```

### قواعد إلزامية للألوان

- `--ansam-blue` **حصري للعناصر القابلة للنقر**. ممنوع استخدامه للزخرفة أو الأيقونات العامة أو الخلفيات. أي شيء بهذا اللون = قابل للنقر.
- `--ansam-gray` تباينه على الخلفية الفاتحة ≈ 3:1، فهو **ممنوع للنص الأساسي**. يُستخدم فقط لتسميات صغيرة ونصوص مساعدة بحجم 12–13px. نص الفقرات يستخدم `--ansam-slate` (تباين 6.5:1).
- خلفية الصفحة `--ansam-cream` وليست الأبيض الصريح. الأقسام تتناوب بين `--ansam-cream` و`--ansam-white`.
- **قسم داكن واحد فقط** في كامل الصفحة بخلفية `--ansam-navy` (قسم الالتزامات التعاقدية / دراسة الحالة). لا تزد عليه.
- ممنوع التدرجات اللونية (gradients) نهائياً. البالتة مسطّحة.

---

## 3. الخطوط

**الخط الأساسي:** Clother Arabic (خط تجاري — استضافة ذاتية).

```css
@font-face {
  font-family: 'Clother Arabic';
  src: url('/fonts/clother-ar-400.woff2') format('woff2');
  font-weight: 400;
  font-display: swap;
}
@font-face {
  font-family: 'Clother Arabic';
  src: url('/fonts/clother-ar-500.woff2') format('woff2');
  font-weight: 500;
  font-display: swap;
}

body {
  font-family: 'Clother Arabic', 'SF Arabic', 'Segoe UI',
               'IBM Plex Sans Arabic', system-ui, sans-serif;
}
```

### قواعد الخط

- وزنان فقط: 400 و500. ممنوع استدعاء أوزان أخرى، وممنوع الاعتماد على `font-weight: bold` الذي يولّده المتصفح صناعياً لأنه يشوّه المحارف العربية.
- الصيغة `woff2` فقط، مع تقليص الملف (subsetting) على النطاقات العربية واللاتينية الأساسية.
- `<link rel="preload" as="font" type="font/woff2" crossorigin>` لوزن 500 فقط.
- الأرقام لاتينية (340 وليس ٣٤٠) مع `font-feature-settings: "tnum"` لمحاذاة الأعمدة.

### مقياس الخط

| العنصر | الحجم (ديسكتوب) | الحجم (جوال) | الوزن | ارتفاع السطر |
|---|---|---|---|---|
| عنوان الهيرو | 48px | 30px | 500 | 1.35 |
| عنوان قسم | 34px | 25px | 500 | 1.4 |
| عنوان بطاقة | 19px | 18px | 500 | 1.5 |
| نص الفقرات | 16px | 15px | 400 | 1.8 |
| تسمية / eyebrow | 13px | 12px | 400 | 1.5 |

`line-height: 1.8` للنص العربي إلزامي — القيمة 1.5 تخنق النص العربي بسبب النقاط والمدّات.

---

## 4. الشعار

| الملف | الاستخدام |
|---|---|
| `ansam-lockup-horizontal.svg` | الهيدر، الفوتر، توقيع الإيميل |
| `ansam-lockup-vertical.svg` | صفحة الدخول، المطبوعات، حسابات السوشال |
| `ansam-mark.svg` (المروحة فقط) | الفافيكون، أيقونة التطبيق، og:image، مؤشر التحميل |

- كل الملفات **SVG مقصوصة على حدود الشكل**. ملفات PNG الأصلية فيها هوامش شفافة واسعة تجعل الشعار يبدو صغيراً وضائعاً — لا تستخدمها في الواجهة.
- ارتفاع الأيقونة في الهيدر 30–32px، والقفل الكامل ≈ 36px.
- المساحة الآمنة حول الشعار = نصف عرض الأيقونة من كل جهة.
- ممنوع تدوير الشعار أو تغيير لونه أو إضافة ظل أو حد له. على الخلفية الكحلية تُستخدم النسخة البيضاء.

---

## 5. لغة الأشكال والعناصر البصرية

الهوية فيها عنصران، ولهما توظيف محدد:

### عنصر الأنابيب (الموجة)
منحنى مستمر بأطراف مدوّرة. يُستخدم **فاصلاً بين الأقسام** بدل الخط المستقيم: SVG بعرض الشاشة، ارتفاع 60–80px، بلون `--ansam-blue-light`، مع `preserveAspectRatio="none"`.

### عنصر المروحة
يظهر **مرة واحدة فقط** في الصفحة كاملة: كبير جداً في زاوية الهيرو، بشفافية 4–6%، مقصوص عند حافة الشاشة. تكراره يحوّله إلى زخرفة رخيصة.

### قواعد الأشكال المشتقة

- **الأزرار كبسولات كاملة الاستدارة** (`border-radius: 999px`) — مشتقة من الكبسولة البيضاء داخل شعار المروحة.
- زوايا البطاقات `16px`، وحقول النماذج `8px`.
- الحدود `0.5px solid var(--ansam-blue-light)`.
- ممنوع الظلال (`box-shadow`) إلا حلقة التركيز على الحقول.
- الأيقونات خطية (outline) بسماكة `2px` مع `stroke-linecap: round` لتتناغم مع أطراف الشعار المدوّرة.

---

## 6. المسافات والشبكة

- كل المسافات على مضاعفات **8px**.
- الحشو الرأسي للأقسام: `96px` ديسكتوب، `56px` جوال.
- أقصى عرض للمحتوى: `1200px`، وأقصى عرض لفقرة نصية: `65ch`.
- استخدم **الخصائص المنطقية** حصراً: `margin-inline-start` بدل `margin-left`، و`padding-inline` بدل `padding-left/right`، و`border-inline-start` — حتى لا تنكسر الواجهة عند إضافة نسخة إنجليزية لاحقاً.

---

## 7. هيكل صفحة الهبوط

1. **الهيدر** — الشعار العرضي، روابط التنقل، زر «طلب عرض سعر».
2. **الهيرو** (خلفية `--ansam-cream`) — عمودان: نص + بطاقة نموذج مختصر (اسم المنشأة، نوع المرفق، رقم الجوال). العنوان عن استمرارية التشغيل لا عن الإصلاح. زر أساسي: «اطلب زيارة تقييم مجانية»، وزر ثانوي: «تحدث مع مهندس».
3. **شريط أرقام الثقة** — أربعة أرقام: سنوات الخبرة، عدد المنشآت المخدومة، زمن الاستجابة التعاقدي، عدد الفنيين المعتمدين.
4. **شعارات العملاء** — بتدرّج رمادي وشفافية 60%، تتلوّن عند المرور. أقوى عنصر تحويل في هذه الصفحة.
5. **الخدمات** — ثلاث بطاقات: تكييف مركزي، تبريد وغرف حفظ، مطابخ تجارية وأفران.
6. **كيف نعمل** — أربع مراحل مرقّمة (الترقيم هنا مبرر لأنها تسلسل فعلي): زيارة تقييم ← عرض فني ومالي ← جدول صيانة وقائية ← تقارير شهرية.
7. **الصيانة الوقائية مقابل الطارئة** — جدول مقارنة: التكلفة السنوية، ساعات التوقف، عمر المعدة. يبرر السعر قبل أن يُسأل عنه.
8. **الالتزامات التعاقدية (SLA)** — القسم الداكن الوحيد.
9. **دراسة حالة** — بأرقام حقيقية.
10. **التراخيص والشهادات** — السجل التجاري والاعتمادات.
11. **نموذج طلب عرض السعر** الكامل.
12. **الأسئلة الشائعة** ثم **الفوتر**.

### قاعدة تحويل

الزر الأساسي في كل الصفحة هو **«اطلب زيارة تقييم مجانية»** ويفتح النموذج. الواتساب ثانوي وليس البطل — قرار الشراء في هذا القطاع لا يُتخذ في نفس الجلسة، والنموذج نفسه دليل جدية.

---

## 8. الحركة (Motion)

### أثر ظهور النص

```css
.reveal-line { display: inline-block; overflow: hidden; vertical-align: bottom; }
.reveal-line > span {
  display: inline-block;
  transform: translateY(115%);
  transition: transform .75s cubic-bezier(.16, 1, .3, 1);
}
.is-visible .reveal-line > span { transform: none; }
```

**قاعدة حرجة للعربية:** التقسيم **بالكلمة أو بالسطر فقط**. ممنوع منعاً باتاً التقسيم بالحرف (`chars`) — لأنه يفصل الحروف العربية عن بعضها ويكسر اتصال الكلمة. أي مكتبة تُستدعى بإعداد `chars` مرفوضة.

**القيم:**
- المنحنى: `cubic-bezier(0.16, 1, 0.3, 1)`
- مدة الكلمات: `750ms` — مدة الفقرات: `600ms`
- التأخير التدريجي بين الكلمات: `55ms` (لا يقل عن 40 ولا يزيد عن 90)
- الفقرة تحت العنوان تظهر ككتلة واحدة بتأخير `550ms`، ولا تُقسّم.

**التشغيل:** `IntersectionObserver` عند `threshold: 0.25`، ثم `observer.unobserve(entry.target)` بعد أول تشغيل. الحركة لا تتكرر عند العودة بالتمرير.

**استثناء الهيرو:** عنوان الهيرو هو عنصر LCP — يظهر فوراً أو بتأخير أقل من `200ms`. أثر الظهور يبدأ من القسم الثاني فما تحت.

### حركة السهم

```css
.card .arrow-icon {
  transition: transform .3s cubic-bezier(.16, 1, .3, 1),
              background .3s, color .3s, border-color .3s;
}
.card:hover .arrow-icon {
  transform: rotate(-45deg);
  background: var(--ansam-blue);
  color: #fff;
  border-color: var(--ansam-blue);
}
```

- الدوران `-45deg` وليس `+45deg` — السهم في الواجهة العربية يشير يساراً، والميلان الصحيح نحو الأعلى-يسار.
- الحركة تُطلق من **البطاقة كاملة** (`.card:hover`) لا من الأيقونة وحدها.
- معالجة واحدة للسهم تُستخدم في كل الموقع. تنويع الحركات بين الأقسام يجعل الموقع يبدو مجمّعاً من قوالب.

### دوران المروحة في الهيرو
دورة كاملة كل `25s`، `linear`, `infinite`. البطء مقصود — السرعة تجعله يبدو رخيصاً.

### تقليل الحركة

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: .01ms !important;
    transition-duration: .01ms !important;
  }
  .reveal-line > span { transform: none !important; }
}
```

إلزامي، وليس اختيارياً.

---

## 9. ممنوعات

- صور المخزون (stock photos) لفنيين مبتسمين. إما صور حقيقية من مواقع العمل، أو لا صور بشرية إطلاقاً. الصور الأجنبية الجاهزة تقتل الثقة في هذا السوق.
- التدرجات اللونية، والظلال، وتأثيرات الوهج والضبابية.
- أي لون خارج البالتة في القسم 2.
- تكرار حركة الظهور عند كل تمرير.
- التقسيم بالحرف في النصوص العربية.
- استخدام `--ansam-gray` لنص أساسي.
- استخدام `--ansam-blue` لأي عنصر غير قابل للنقر.
- الخصائص الاتجاهية (`margin-left`, `padding-right`) بدل المنطقية.

---

## 10. قائمة التحقق قبل اعتماد أي شاشة

- [ ] تعمل على عرض 375px بدون تمرير أفقي
- [ ] كل نص أساسي بتباين 4.5:1 فأعلى
- [ ] التركيز بلوحة المفاتيح ظاهر على كل عنصر تفاعلي
- [ ] `prefers-reduced-motion` مُحترمة
- [ ] كل الأيقونات الزخرفية عليها `aria-hidden="true"`، وكل زر أيقوني عليه `aria-label`
- [ ] الخط الاحتياطي لا يسبب قفزة في التخطيط عند التحميل
- [ ] لا يوجد لون خارج البالتة المعتمدة
- [ ] الشعار SVG مقصوص وليس PNG بهوامش
