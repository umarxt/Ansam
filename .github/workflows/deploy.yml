name: Deploy to Cloudflare

# ينشر الموقع تلقائياً على الإنتاج عند كل دفع/دمج على main.
# يتطلب سرّين في إعدادات المستودع (Settings → Secrets and variables → Actions):
#   - CLOUDFLARE_API_TOKEN   (إلزامي)
#   - CLOUDFLARE_ACCOUNT_ID  (اختياري — يلزم فقط إن كان للتوكن وصول لأكثر من حساب)
on:
  push:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build frontend
        run: npm run build

      - name: Deploy to Cloudflare
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy
